services:
  - type: web
    name: auth-sync-service-dev
    runtime: docker
    plan: starter
    region: frankfurt
    branch: dev
    dockerfilePath: ./deploy/Dockerfile
    envVars:
      - key: CELERY_BROKER
        fromService:
          name: celery-redis
          type: redis
          property: connectionString
      - fromGroup: auth-sync-service

  - type: redis
    name: celery-redis
    region: frankfurt
    plan: free
    maxmemoryPolicy: noeviction
    ipAllowList: []

envVarGroups:
  - name: auth-sync-service
    envVars:
      - key: TELEGRAM_TOKEN
        value: tg_token
      - key: COROS_API_URL
        value: https://teameuapi.coros.com
      - key: COROS_EMAIL
        value: user_email
      - key: COROS_PASSWORD
        value: user_password
      - key: GARMIN_CONNECT_EMAIL
        value: user_email
      - key: GARMIN_CONNECT_PASSWORD
        value: user_password
